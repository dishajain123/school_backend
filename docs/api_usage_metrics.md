# API Usage Metrics

Source: in-memory runtime tracker summary in `app/core/api_usage_tracker.py`.

- Most-used endpoints: available after runtime traffic is observed.
- Least-used endpoints: available after runtime traffic is observed.
- Error rate per endpoint: available after runtime traffic is observed.

Current run note: no live request traffic captured in this process, so metrics are pending runtime data.
